% Build metadata — committed placeholder so local/Overleaf compiles always work.
% CI (.github/workflows/build-resume.yml) overwrites this file in the workspace
% with the real git short-SHA and UTC build date; the overwrite is never committed.
\newcommand{\BuildHash}{draft}
\newcommand{\BuildDate}{\today}
\newcommand{\VerifyURL}{https://ammar-portfolio-zeta.vercel.app/verify}
